[pytest]
pythonpath = .
testpaths = tests

# Enable coverage reporting for both app/ and main.py
addopts = --cov=app --cov=main --cov-report=term-missing

markers =
    no_disable_mp: Skip the disable_multiprocessing fixture patch, allowing real multiprocessing code
